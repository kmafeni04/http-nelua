local http = require "...http"

local app = http.new()

app:set_static("./static", "static")

app:get("index", "/", function(self: *http.Server)
  return self:text(200, "hello")
end)

app:get("bye", "/bye", function(self: *http.Server)
  return self:text(200, "bye")
end)

app:serve()
